doctype
html
    head
        meta(charset='utf-8')
        title Gif 채팅방
    body
        div F12를 눌러 조이를 표하시오
        script(src="/socket.io/socket.io.js")
        script.
            var socket=io.connect('http://localhost',{path:'/socket.io'});
            socket.on('news',d=>{
                console.log(d);
                socket.emit('reply','hello js');
            });
